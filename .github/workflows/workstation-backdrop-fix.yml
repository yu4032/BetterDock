name: Verify workstation backdrop visibility

on:
  push:
    branches: [ fix/workstation-backdrop-visibility ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle
      - uses: gradle/actions/setup-gradle@v4
      - run: chmod +x ./gradlew
      - name: Workstation backdrop regression
        run: ./gradlew testDebugUnitTest --tests com.hellovoid.liquiddock.WorkstationBackdropVisibilityContractTest --stacktrace
      - name: Full unit suite
        run: ./gradlew testDebugUnitTest --stacktrace
      - name: Build debug APK
        run: ./gradlew assembleDebug --stacktrace
      - uses: actions/upload-artifact@v4
        with:
          name: LiquidDock-workstation-backdrop-debug
          path: build/outputs/apk/debug/*.apk
          if-no-files-found: error
          retention-days: 7
