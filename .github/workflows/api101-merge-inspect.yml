name: API101 merge helper (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automatic API101 merge helper is disabled."
