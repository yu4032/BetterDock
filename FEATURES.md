# LiquidDock 功能手册

## 桌面网格 (Grid)

调整 8×4 横屏 / 4×8 竖屏布局的图标位置与间距。

| 参数 | 范围 | 说明 |
|------|------|------|
| 横屏水平距离偏移 | −600 ~ 600 dp | 同时调整横屏左右两侧水平距离 |
| 横屏顶部距离偏移 | −600 ~ 600 dp | 相对扣除 Dock 后的可用区域调顶部 |
| 横屏底部距离偏移 | −600 ~ 600 dp | 相对扣除 Dock 后的可用区域调底部 |
| 竖屏水平距离偏移 | −600 ~ 600 dp | 同时调整竖屏左右两侧水平距离 |
| 竖屏顶部距离偏移 | −600 ~ 600 dp | 相对扣除 Dock 后的可用区域调顶部 |
| 竖屏底部距离偏移 | −600 ~ 600 dp | 相对扣除 Dock 后的可用区域调底部 |
| 横屏图标纵向间距偏移 | −200 ~ 400 dp | 增减横屏图标行之间的垂直距离 |
| 竖屏图标纵向间距偏移 | −200 ~ 400 dp | 增减竖屏图标行之间的垂直距离 |
| 横屏指示器 Y | −160 ~ 160 dp | 横屏页面指示器垂直位置 |
| 竖屏指示器 Y | −160 ~ 160 dp | 竖屏页面指示器垂直位置 |

---

## Dock 外观

控制 Dock 背景尺寸、圆角、间距和模糊。

| 参数 | 范围 | 说明 |
|------|------|------|
| 模糊强度 | 0 ~ 400 | 原生模糊模式下的 Dock 模糊量（液态玻璃用独立参数） |
| 高度偏移 | −80 ~ 80 dp | 相对默认高度增减 Dock 背景高度 |
| 宽度偏移 | −80 ~ 80 dp | 相对默认宽度增减 Dock 背景长度 |
| 描边圆角偏移 | −1 ~ 100 dp | 相对默认值调整外部描边圆角（−1 为系统默认） |
| 内部模糊圆角偏移 | −50 ~ 100 dp | 单独调整内部模糊背景圆角 |
| Dock 图标间距 | −8 ~ 12 dp | 增减相邻 Dock 图标之间的距离 |
| Dock 底部偏移 | −30 ~ 40 dp | 调整 Dock 与屏幕底部的距离 |

### 分隔竖线（工作台模式）

| 参数 | 范围 | 说明 |
|------|------|------|
| 分隔线宽度 | 0 ~ 160 (dp×10) | 竖线绝对宽度，0 使用系统默认 |
| 分隔线高度比例 | 0 ~ 100 % | 竖线占图标高度的百分比，0 使用系统默认 |
| 分隔线垂直偏移 | −80 ~ 80 (dp×10) | 上下偏移，正值下移负值上移 |
| 颜色 · 红 | 0 ~ 255 | RGB 全 0 使用系统默认 |
| 颜色 · 绿 | 0 ~ 255 | |
| 颜色 · 蓝 | 0 ~ 255 | |
| 透明度 | 0 ~ 255 | 0 使用系统默认 |

---

## 工作台 (Workstation)

仅在工作台/笔记本模式下生效，独立于普通桌面。

| 参数 | 范围 | 说明 |
|------|------|------|
| 工作台 Dock 长度偏移 | −240 ~ 240 dp | 相对系统工作台 Dock 原始长度增减 |
| 工作台桌面水平偏移 | −240 ~ 240 dp | 8 列图标区域左右距离，不继承普通桌面 |
| 所有应用 · 横屏水平偏移 | −240 ~ 240 dp | 仅工作台所有应用横屏图标区水平位置 |
| 所有应用 · 横屏垂直偏移 | −240 ~ 240 dp | 仅工作台所有应用横屏图标区垂直位置 |
| 所有应用 · 竖屏水平偏移 | −240 ~ 240 dp | 仅工作台所有应用竖屏图标区水平位置 |
| 所有应用 · 竖屏垂直偏移 | −240 ~ 240 dp | 仅工作台所有应用竖屏图标区垂直位置 |
| 工作台图标上间距 | −48 ~ 48 dp | 图标与容器顶部间距 |
| 工作台图标下间距 | −48 ~ 48 dp | 图标与容器底部间距 |

---

## 液态玻璃 (Liquid)

光学模型参数（Prismal 参考实现）。

### 核心光学

| 参数 | 范围 | 说明 |
|------|------|------|
| 玻璃模糊 | 0 ~ 60 dp | 对捕获背景的模糊范围 |
| 玻璃厚度 | 1 ~ 60 dp | 影响折射路径的虚拟玻璃厚度 |
| 折射率 IOR | 100 ~ 200 % | 越高，边缘弯曲越明显 |
| 法线强度 | 0 ~ 300 % | 表面法线起伏对折射的影响 |
| 穹顶凸起 | 0 ~ 200 % | 控制玻璃中心向外凸起的程度 |
| 透镜折射 | 0 ~ 60 dp | 圆角边缘的透镜偏移距离 |
| 色散强度 | 0 ~ 40 % | 边缘红蓝通道分离强度 |

### 颜色与基底

| 参数 | 范围 | 说明 |
|------|------|------|
| 玻璃底色透明度 | 0 ~ 160 | 玻璃底色覆盖背景的强度 |
| 底色 · 红 | 0 ~ 255 | |
| 底色 · 绿 | 0 ~ 255 | |
| 底色 · 蓝 | 0 ~ 255 | |
| 亮度 | 50 ~ 200 % | 整体调整输出亮度 |

### 高光与边缘

| 参数 | 范围 | 说明 |
|------|------|------|
| 边缘高光厚度 | 20 ~ 300 % | 外圈高光描边的粗细 |
| 高光不透明度 | 0 ~ 200 % | 外圈高光描边的明暗 |
| 镜面高光锐度 | 0 ~ 100 % | 越高越集中且边缘更硬 |
| 镜面高光强度 | 0 ~ 200 % | 表面镜面高光亮度 |
| 边缘受光 | 0 ~ 100 % | 玻璃边缘受光强度 |
| 焦散 | 0 ~ 100 % | 折射聚光形成的亮斑 |
| 边缘光带宽度 | 0 ~ 200 % | Shader 边缘光带覆盖宽度 |
| 深度透镜效果 | 0 ~ 50 % | 中心到边缘的深度变化 |
| 原生模糊内缩 | 0 ~ 16 dp | 向内收缩，为外圈高光留出间距 |

---

## 描边 (Stroke)

Dock 轮廓描边。

| 参数 | 范围 | 说明 |
|------|------|------|
| 描边底色 · 红 | 0 ~ 255 | 需开启 `dock_stroke` 开关 |
| 描边底色 · 绿 | 0 ~ 255 | |
| 描边底色 · 蓝 | 0 ~ 255 | |
| 描边底色 · 透明度 | 0 ~ 255 | |
| 方圆形描边宽度 | 1 ~ 10 dp | 方圆形模式下的描边宽度 |
| 方圆形内缩量 | 0 ~ 50 dp | 相对 Dock 边界的内缩 |
| 方圆形控制点 | 0 ~ 100 | 直边过渡圆角的形状 |
| Fill-Diff 宽度 | 0 ~ 30 dp | 外层与挖空层之间的宽度 |
| 标准描边宽度 | 0 ~ 30 dp | 普通路径描边线宽 |

---

## 阴影 (Shadow)

| 参数 | 范围 | 说明 |
|------|------|------|
| Dock 阴影柔和度 | 0 ~ 100 dp | 整个 Dock 阴影边缘柔化 |
| Dock 阴影扩散 | 0 ~ 120 dp | 阴影向外扩散最大距离 |
| Dock 阴影浓度 | 0 ~ 255 | 整个 Dock 下方阴影浓度 |
| Dock 阴影 Y 偏移 | −60 ~ 60 dp | 垂直偏移，可为负数 |
| 描边阴影柔化 | 0 ~ 30 dp | 仅描边阴影柔化半径 |
| 描边阴影不透明度 | 0 ~ 255 | 仅描边阴影透明度 |

---

## 捕获 (Capture)

控制液态玻璃的屏幕捕获行为。

| 参数 | 范围 | 说明 |
|------|------|------|
| 捕获帧率上限 | 1 ~ 120 fps | 动画和动态应用的统一帧率上限 |
| 静态探针帧率 | 1 ~ 30 fps | 静态应用画面变化检测的低频帧率 |
| 动态运动阈值 | 1 ~ 128 | 越低越容易因亮度变化进入高频捕获 |
| 动态像素阈值 | 1 ~ 64 | 越低越容易因采样像素变化触发高频捕获 |
| 高频保持时间 | 0 ~ 5000 ms | 检测到动态后维持高频捕获的时长 |
| 黑帧亮度阈值 | 0 ~ 64 | 低于此平均亮度的帧被丢弃 |
| 主页壁纸捕获延迟 | 200 ~ 3000 ms | APP→HOME 返回后等待壁纸稳定的时间 |
| 捕获分辨率 | 10 ~ 100 % | SurfaceFlinger 捕获分辨率 |
| 捕获停止延迟 | 0 ~ 10000 ms | Dock 隐藏后继续允许捕获的缓冲 |
| 多任务预触发距离 | 1 ~ 48 dp | 底部上滑达到此距离时提前启动 RECENTS 捕获 |
| 上额外捕获高度 | 0 ~ 256 dp | Dock 上方多捕获的折射取样区域 |
| 下额外捕获高度 | 0 ~ 256 dp | Dock 下方多捕获的折射取样区域 |

---

## 配置管理

- **导出/导入**：Data 页面一键导出/导入所有参数为 JSON
- **预设**：匹配推荐布局的默认预设
- **后台同步**：LSPosed Remote Preferences (Binder IPC)，无需 root 或 su
- **热重载**：修改参数后 1 秒内自动生效，无需重启 Launcher
